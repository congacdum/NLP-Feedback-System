APP_SECRET=replace-with-a-long-random-secret
NLP_BACKEND=demo
DEV_DEMO_CATALOG=false
ISSUE_EXTRACTION_ENABLED=true
# TRANSFORMER_ARTIFACT=/app/model_artifacts/phobert_absa_final
# VNCORENLP_DIR=/app/vendor/vncorenlp
# EVALUATION_ARTIFACT=model_artifacts/phobert_absa_final

# Default lightweight runtime uses the bundled classical baseline.
# After a frozen Transformer artifact exists, set this to
# requirements-transformer-runtime.txt and configure TRANSFORMER_ARTIFACT.
REQUIREMENTS_FILE=requirements.txt
